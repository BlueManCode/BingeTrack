import React from 'react';

type Props = {
  icon: any;
  title: string;
  navigate: () => {};
};

const IconSideBarOption = (props: Props) => {
  return (
    <>
      <div>IconSideBarOption</div>
    </>
  );
};

export default IconSideBarOption;
